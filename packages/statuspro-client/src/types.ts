/**
 * Types-only exports for StatusPro OpenAPI Client
 *
 * Import from this module when you only need TypeScript types
 * without any runtime code (e.g., for type declarations in other packages).
 *
 * @example
 * ```typescript
 * import type { OrderListItem, Status, OrderResponse } from '@statuspro/client/types';
 * ```
 */

// Re-export all generated types
export type * from './generated/types.gen.js';
